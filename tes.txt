import React from 'react';
import { motion } from 'framer-motion';
import { FaGithub, FaLinkedin, FaEnvelope, FaBriefcase, FaCode, FaExternalLinkAlt, FaTerminal } from 'react-icons/fa';
import data from '../data/portfolio.json';
import profileImg from '../assets/profile.png';

const cardVariants = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { type: 'spring', stiffness: 100, damping: 15 }
  },
  hover: {
    scale: 0.98,
    transition: { type: 'spring', stiffness: 400, damping: 10 }
  }
};

const BentoGrid = () => {
  return (
    <div className="min-h-screen bg-[#f5f5f7] bg-noise py-6 px-4 md:px-8 font-sans text-zinc-900">
      <div className="max-w-6xl mx-auto flex flex-col gap-4 md:gap-6">

        {/* ========================================== */}
        {/* BARIS 1: FOTO & PROFIL                     */}
        {/* ========================================== */}

        {/* === MOBILE: Hero Stack Layout === */}
        <div className="flex flex-col md:hidden gap-0 rounded-[2rem] overflow-hidden shadow-sm border border-zinc-200/50">
          {/* Photo — full width, cinematic crop */}
          <div className="relative h-56 bg-zinc-200 overflow-hidden">
            <img
              src={profileImg}
              alt={data.profile.name}
              className="w-full h-full object-cover object-top"
            />
            <div className="absolute inset-0 bg-gradient-to-t from-black/70 via-black/20 to-transparent" />
            {/* Role badge overlay */}
            <div className="absolute top-4 left-4">
              <span className="text-xs font-bold tracking-widest uppercase text-white/80 bg-white/10 backdrop-blur-md px-3 py-1 rounded-full border border-white/20">
                {data.profile.role}
              </span>
            </div>
            {/* Name overlaid on photo bottom */}
            <div className="absolute bottom-0 left-0 right-0 px-6 pb-5">
              <h1 className="text-3xl font-extrabold tracking-tight text-white leading-tight">
                Hi, I'm<br />{data.profile.name}.
              </h1>
            </div>
          </div>

          {/* Bio + Social */}
          <div className="bg-white/90 backdrop-blur-xl px-6 py-5 flex flex-col gap-4">
            <p className="text-base text-zinc-500 leading-relaxed">
              {data.profile.tagline}
            </p>
            <div className="flex gap-3">
              <a href={data.profile.contact.email}
                className="flex items-center gap-2 px-4 py-2.5 bg-zinc-900 text-white rounded-full text-sm font-semibold transition-colors active:scale-95">
                <FaEnvelope size={14} /> Email
              </a>
              <a href={data.profile.contact.github} target="_blank" rel="noreferrer"
                className="p-2.5 bg-zinc-100 hover:bg-zinc-200 text-zinc-700 rounded-full transition-colors active:scale-95">
                <FaGithub size={20} />
              </a>
              <a href={data.profile.contact.linkedin} target="_blank" rel="noreferrer"
                className="p-2.5 bg-zinc-100 hover:bg-zinc-200 text-zinc-700 rounded-full transition-colors active:scale-95">
                <FaLinkedin size={20} />
              </a>
            </div>
          </div>
        </div>

        {/* === DESKTOP: Original Bento Row === */}
        <div className="hidden md:grid md:grid-cols-6 gap-4 md:gap-6 auto-rows-[240px]">
          <motion.div
            variants={cardVariants} initial="hidden" animate="visible" whileHover="hover"
            className="md:col-span-2 relative rounded-[2rem] overflow-hidden shadow-sm border border-zinc-200/50 bg-zinc-200 group"
          >
            <img
              src={profileImg}
              alt={data.profile.name}
              className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700"
            />
            <div className="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-60" />
          </motion.div>

          <motion.div
            variants={cardVariants} initial="hidden" animate="visible" whileHover="hover"
            className="md:col-span-4 bg-white/80 backdrop-blur-xl rounded-[2rem] p-8 md:p-10 border border-white shadow-sm flex flex-col justify-center"
          >
            <p className="text-sm font-bold tracking-widest uppercase text-zinc-400 mb-2">{data.profile.role}</p>
            <h1 className="text-4xl md:text-5xl font-extrabold tracking-tight mb-4">
              Hi, I'm {data.profile.name}.
            </h1>
            <p className="text-lg text-zinc-500 max-w-lg leading-relaxed mb-6">
              {data.profile.tagline}
            </p>
            <div className="flex gap-4 mt-auto">
              <a href={data.profile.contact.email} className="p-3 bg-zinc-100 hover:bg-zinc-200 text-zinc-700 rounded-full transition-colors">
                <FaEnvelope size={20} />
              </a>
              <a href={data.profile.contact.github} target="_blank" rel="noreferrer" className="p-3 bg-zinc-100 hover:bg-zinc-200 text-zinc-700 rounded-full transition-colors">
                <FaGithub size={20} />
              </a>
              <a href={data.profile.contact.linkedin} target="_blank" rel="noreferrer" className="p-3 bg-zinc-100 hover:bg-zinc-200 text-zinc-700 rounded-full transition-colors">
                <FaLinkedin size={20} />
              </a>
            </div>
          </motion.div>
        </div>

        {/* ========================================== */}
        {/* BARIS 2: EXPERIENCE & TECH STACK           */}
        {/* ========================================== */}
        <div className="grid grid-cols-1 md:grid-cols-2 gap-4 md:gap-6">

          {/* Experience */}
          <motion.div
            variants={cardVariants} initial="hidden" animate="visible" whileHover="hover"
            className="bg-white rounded-[2rem] p-6 md:p-8 border border-zinc-200 shadow-sm flex flex-col"
          >
            <div className="flex items-center gap-3 mb-5 text-zinc-800">
              <div className="p-2 bg-zinc-100 rounded-xl">
                <FaBriefcase size={18} />
              </div>
              <h2 className="text-xl md:text-2xl font-bold">Experience</h2>
            </div>
            {data.experience.map((exp, index) => (
              <div key={index} className="flex flex-col gap-1 border-l-2 border-zinc-200 pl-4 ml-2 mb-4 last:mb-0">
                <h3 className="text-base md:text-lg font-bold text-zinc-900">{exp.role}</h3>
                <p className="text-sm md:text-base text-zinc-500 font-medium">
                  {exp.company} <span className="text-zinc-400 font-normal">({exp.period})</span>
                </p>
                <ul className="mt-1.5 flex flex-col gap-1">
                  {exp.tasks.map((task, i) => (
                    <li key={i} className="text-sm text-zinc-600 flex items-start gap-2">
                      <span className="w-1.5 h-1.5 bg-zinc-300 rounded-full mt-1.5 shrink-0"></span>
                      {task}
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </motion.div>

          {/* Tech Stack */}
          <motion.div
            variants={cardVariants} initial="hidden" animate="visible" whileHover="hover"
            className="bg-zinc-900 text-white rounded-[2rem] p-6 md:p-8 border border-zinc-800 shadow-lg flex flex-col"
          >
            <div className="flex items-center gap-3 mb-5 text-zinc-100">
              <div className="p-2 bg-zinc-800 rounded-xl">
                <FaCode size={18} />
              </div>
              <h2 className="text-xl md:text-2xl font-bold">Tech Stack</h2>
            </div>
            {/* Mobile: vertical list, Desktop: 3-col grid */}
            <div className="flex flex-col md:grid md:grid-cols-3 gap-5">
              {Object.entries(data.technical_stack).map(([category, items]) => (
                <div key={category}>
                  <h4 className="text-xs uppercase tracking-widest text-zinc-500 font-bold mb-2">{category}</h4>
                  <div className="flex flex-wrap gap-2">
                    {items.map((item, i) => (
                      <span key={i} className="px-3 py-1 bg-zinc-800 border border-zinc-700 text-zinc-300 rounded-lg text-sm">
                        {item}
                      </span>
                    ))}
                  </div>
                </div>
              ))}
            </div>
          </motion.div>
        </div>

        {/* ========================================== */}
        {/* BARIS 3: PROJECTS                          */}
        {/* ========================================== */}

        {/* === MOBILE: Swipeable Horizontal Scroll === */}
        <div className="flex flex-col md:hidden gap-3">
          <div className="flex items-center justify-between px-1 mb-1">
            <h2 className="text-xl font-bold tracking-tight text-zinc-900">Projects</h2>
            <span className="text-xs text-zinc-400 font-medium">swipe to explore →</span>
          </div>

          {/* Horizontal scroll strip */}
          <div
            className="flex gap-4 overflow-x-auto pb-4 snap-x snap-mandatory scrollbar-hide"
            style={{ scrollbarWidth: 'none', WebkitOverflowScrolling: 'touch' }}
          >
            {data.projects.map((project, index) => (
              <div
                key={index}
                className="snap-start shrink-0 w-[82vw] max-w-[320px] bg-gradient-to-br from-zinc-900 to-zinc-800 rounded-[1.75rem] p-6 text-white flex flex-col justify-between relative overflow-hidden shadow-xl"
              >
                {/* Decorative icon */}
                <FaTerminal className="absolute -right-6 -bottom-6 w-28 h-28 text-white opacity-5 pointer-events-none" />

                <div className="z-10 flex flex-col gap-3">
                  {/* Category badge */}
                  <span className="self-start text-xs font-mono text-emerald-400 bg-emerald-400/10 border border-emerald-400/20 px-2.5 py-1 rounded-full">
                    {project.category}
                  </span>

                  {/* Project number */}
                  <span className="text-5xl font-black text-white/5 -mt-1 leading-none select-none">
                    {String(index + 1).padStart(2, '0')}
                  </span>

                  <h3 className="text-xl font-bold leading-snug -mt-2">{project.title}</h3>
                  <p className="text-sm text-zinc-400 leading-relaxed line-clamp-3">
                    {project.description}
                  </p>

                  {/* Stack pills */}
                  <div className="flex flex-wrap gap-1.5 mt-1">
                    {project.stack.slice(0, 4).map((tech, i) => (
                      <span key={i} className="text-[11px] font-mono bg-white/10 px-2 py-1 rounded-full text-zinc-300">
                        {tech}
                      </span>
                    ))}
                    {project.stack.length > 4 && (
                      <span className="text-[11px] font-mono bg-white/5 px-2 py-1 rounded-full text-zinc-500">
                        +{project.stack.length - 4}
                      </span>
                    )}
                  </div>
                </div>

                {/* CTA buttons */}
                <div className="z-10 flex gap-2 mt-5">
                  <a
                    href={project.live_demo}
                    className="flex-1 flex items-center justify-center gap-1.5 px-4 py-2.5 bg-white text-zinc-900 rounded-full text-sm font-bold hover:bg-zinc-200 active:scale-95 transition-all"
                  >
                    <FaExternalLinkAlt size={12} /> Demo
                  </a>
                  <a
                    href={project.repository}
                    className="flex items-center justify-center gap-1.5 px-4 py-2.5 bg-zinc-800 text-white border border-zinc-700 rounded-full text-sm font-medium hover:bg-zinc-700 active:scale-95 transition-all"
                  >
                    <FaGithub size={14} />
                  </a>
                </div>
              </div>
            ))}
          </div>

          {/* Scroll indicator dots */}
          <div className="flex justify-center gap-1.5 mt-1">
            {data.projects.map((_, i) => (
              <div
                key={i}
                className={`rounded-full transition-all duration-300 ${i === 0 ? 'w-5 h-1.5 bg-zinc-800' : 'w-1.5 h-1.5 bg-zinc-300'}`}
              />
            ))}
          </div>
        </div>

        {/* === DESKTOP: Original Stacked Cards === */}
        <div className="hidden md:flex md:flex-col gap-4 md:gap-6">
          {data.projects.map((project, index) => (
            <motion.div
              key={index}
              variants={cardVariants} initial="hidden" animate="visible" whileHover="hover"
              className="bg-gradient-to-br from-zinc-900 to-zinc-800 rounded-[2rem] p-8 md:p-10 text-white flex flex-col md:flex-row items-center justify-between relative overflow-hidden shadow-xl"
            >
              <FaTerminal className="absolute -right-10 -bottom-10 w-64 h-64 text-white opacity-5 pointer-events-none" />

              <div className="z-10 w-full md:w-2/3">
                <p className="text-emerald-400 font-mono text-sm mb-2">{project.category}</p>
                <h3 className="text-3xl md:text-4xl font-bold mb-4">{project.title}</h3>
                <p className="text-zinc-400 text-lg leading-relaxed mb-6">{project.description}</p>
                <div className="flex flex-wrap gap-2 mb-8">
                  {project.stack.map((tech, i) => (
                    <span key={i} className="text-xs font-mono bg-white/10 px-3 py-1.5 rounded-full backdrop-blur-md">
                      {tech}
                    </span>
                  ))}
                </div>
              </div>

              <div className="z-10 w-full md:w-auto flex flex-row md:flex-col gap-4 mt-6 md:mt-0">
                <a href={project.live_demo} className="flex-1 md:flex-none flex items-center justify-center gap-2 px-6 py-3 bg-white text-zinc-900 rounded-full font-bold hover:bg-zinc-200 transition-colors">
                  <FaExternalLinkAlt size={18} /> Live Demo
                </a>
                <a href={project.repository} className="flex-1 md:flex-none flex items-center justify-center gap-2 px-6 py-3 bg-zinc-800 text-white border border-zinc-700 rounded-full font-medium hover:bg-zinc-700 transition-colors">
                  <FaGithub size={18} /> Source Code
                </a>
              </div>
            </motion.div>
          ))}
        </div>

      </div>
    </div>
  );
};

export default BentoGrid;